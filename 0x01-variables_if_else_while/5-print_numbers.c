#include <stdio.h>

/**
 * main - Prints alphabet in lowercase except q and e followed by a new line
 * Return: 0 if it's true
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
