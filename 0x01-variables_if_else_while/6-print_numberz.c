#include <stdio.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints 
 * Return: 0 if it's true
 */

int main(void)
{
	int i;

	for (i = '0'; i < '10'; i++)
		putchar(i);
	putchar(\n);
	return (0):
}
