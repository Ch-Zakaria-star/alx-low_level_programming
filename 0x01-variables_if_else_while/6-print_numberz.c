#include <stdio.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints 0 to 9 numbers
 *
 * Return: 0 if it's true
 */

int main(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
